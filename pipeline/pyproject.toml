[project]
name = "mkt-pipeline"
version = "0.1.0"
description = "Marketing Intelligence ingestion pipelines"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.2.0",
    "sqlalchemy>=2.0.0",
    "psycopg2-binary>=2.9.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.6.0",
    "pytest>=8.0.0",
]